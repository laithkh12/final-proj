# Browser requests use /api (same-origin). Next.js rewrites /api → backend.
NEXT_PUBLIC_API_URL=/api

# Dev: local Express. Prod (Vercel): set to your Render URL without /api suffix.
API_PROXY_URL=http://localhost:5000
