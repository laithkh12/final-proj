# Browser requests use /api (same-origin). The catch-all route in src/app/api proxies to the backend.
NEXT_PUBLIC_API_URL=/api

# Dev: local Express. Prod (Vercel): full Render URL — NOT /api
# Example: https://final-proj-yjse.onrender.com
API_PROXY_URL=http://localhost:5000
